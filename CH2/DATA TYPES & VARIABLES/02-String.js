const msg = "Hello";
const place = "World";

// logging out a string
console.log('Hello, World');
// Using substitutions
console.log('Hellow, %s!', place);

// Using a string literal
console.log('${msg},${place}!');
